<!-- This is used in JBOSS 6 in order to try and isolate the classloader
      Due to some issues, we still need to move our ASM-3.1 to the JBoss 
      endorsed lib -->
<classloading xmlns="urn:jboss:classloading:1.0" name="identityiq.war"
    domain="DefaultDomain" parent-first="false" parent-domain="Ignored"
    export-all="NON_EMPTY" import-all="true">
</classloading>